const {chromium}=require('playwright');const fs=require('fs');const serve=require('./preview.cjs');
(async()=>{fs.mkdirSync('tmp/design-studio',{recursive:true});const server=serve();const browser=await chromium.launch({channel:'chrome',headless:true});try{for(const width of [1440,390]){const page=await browser.newPage({viewport:{width,height:1000},reducedMotion:'reduce'});await page.goto('http://127.0.0.1:8766/hoea_to_waka/');await page.waitForTimeout(600);await page.screenshot({path:`tmp/design-studio/before-${width}.png`,fullPage:true});await page.close();}}finally{await browser.close();server.close();}})();
